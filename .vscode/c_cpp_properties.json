{
    "configurations": [
        {
            "includePath": [
                "${env:PICO_SDK_PATH}/**",
                "${env:HOME}/esp/esp-idf/components/**",
                "${workspaceFolder}/**"  // edit this too, or the includes in your project directory instead become unrecognised
            ],
            "defines": [ "PICO_SDK=0", "ESP32_SDK=1", "FREE_RTOS_KERNEL_SMP=1",
                "SPI_TO_FPGA=0", "BME680_PRESENT=1"
            ]
        }
    ],
    "version": 4
}